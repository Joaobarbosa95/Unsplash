const main = document.querySelector("main");
const button = document.querySelector(".delete");
const label = document.querySelector("label");

button.addEventListener("click", (e) => {
  // form visible
  // new img container div with form info
  // render new page
});
